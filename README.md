# Floppy OS

## Installation

Activate the platformio environment at

unix:

```bash
source ~/.platformio/penv/bin/activate
```

windows:

```bash
source ~/.platformio/penv/bin/Activate.ps1
```

Install the dependencies

```bash
pip install python-dotenv
```
